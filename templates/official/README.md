# Official blanks

- Files here are **structural DOCX** generated by `scripts/build-official-templates.py` unless a sibling `*.from-drive` marker exists.
- When an official Drive download is saved, write the real `.docx` and touch `NAME.docx.from-drive` so the generator will not overwrite it.
- Generation: official copy + appended „Данни от портала“, **or** docxtemplater fill when the file has `{fullName}` / `{faculty}` placeholders (structural templates).
- Pixel-perfect Word bookmark mapping is **not** claimed. Next pass: ROUTES.docx form-by-form.
